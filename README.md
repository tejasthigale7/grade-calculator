# 🎓 Grade Calculator using Python (Loops & Functions)

A modular **Python command-line grade calculator** that collects student scores, computes averages, assigns letter grades, and generates a formatted summary report.

This project demonstrates core Python concepts such as **loops, functions, dictionaries, input validation, and formatted output**.

---

## 🚀 Features

- Accepts multiple students and their grades
- Validates numeric input (0–100)
- Calculates average score per student
- Assigns letter grades (A–F)
- Displays a clean formatted summary table
- Identifies highest and lowest averages
- Modular design using helper functions

---

## 🧠 Concepts Used

- `while` and `for` loops
- Functions and modular programming
- Dictionaries and lists
- Input validation with `try-except`
- String formatting (`f-strings`)
- Built-in functions (`max`, `min`)

---

## 📂 Project Structure

grade-calculator/
│
├── grade_calculator.py # Main script
├── utils.py # Helper functions
├── README.md # Documentation
└── .gitignore
